plugins {
    id("com.starter.config")
    id("com.starter.library.android") apply false
}

commonConfig {
    javaFilesAllowed = false
}

if (gradle.gradleVersion.startsWith("9")) {
    allprojects {
        it.tasks.withType(Test).configureEach { it.failOnNoDiscoveredTests = false }
    }
}
